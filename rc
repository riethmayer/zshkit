zstyle ':completion:*' completer _expand _complete

autoload -Uz compinit
compinit

autoload -Uz colors
colors

# if rvm present, use it
if [ -s ~/.rvm/scripts/rvm ] ; then source ~/.rvm/scripts/rvm ; fi

